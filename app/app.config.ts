export default defineAppConfig({
  ui: {
    colors: {
      neutral: "neutral",
      primary: "pink",
    },
    input: {
      slots: {
        root: "flex",
      },
      defaultVariants: {
        size: "lg",
      },
    },
    button: {
      defaultVariants: {
        size: "lg",
      },
    },
  },
});
